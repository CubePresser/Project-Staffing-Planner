<!--Gets the total cost of a single month for one or many projects-->
<h3>Get cost of a single month in a project</h3>
<form id="q0_form" action="home/q0" method="POST">
    Month (Choose one): <br>
    <select id="q0_month" name="month" class="months" required>
        <option value="1">January</option>
        <option value="2">February</option>
        <option value="3">March</option>
        <option value="4">April</option>
        <option value="5">May</option>
        <option value="6">June</option>
        <option value="7">July</option>
        <option value="8">August</option>
        <option value="9">September</option>
        <option value="10">October</option>
        <option value="11">November</option>
        <option value="12">December</option>
    </select><br>
    Project (Choose one): <br>
    <select id="q0_project" name="project" class="projects" required> <!--These are added whenever the user adds one to the database-->
        {{#each project}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q0_result"></span>
</form>

<!--Gets total costs of roles for a single project-->
<h3>Total cost of all instances of a role for a single project</h3>
<form id="q1_form" action="home/q1" method="POST">
    Role (Choose one): <br>
    <select id="q1_role" name="role" class="roles" required> <!--These are added whenever the user adds one to the database-->
        {{#each role}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    Project (Choose one): <br>
    <select id="q1_project" name="project" class="projects" required>
        {{#each project}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q1_result"></span>
</form>

<!--Gets total cost of a single company for a single project-->
<h3>Total cost of a company for a single project</h3>
<form id="q2_form" action="home/q2" method="POST">
    Company (Choose one): <br>
    <select id="q2_company" name="company" class="companies" required>
        {{#each company}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    Project (Choose one): <br>
    <select id="q2_project" name="project" class="projects" required>
        {{#each project}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q2_result"></span>
</form>

<h3>Roles associated with a single company</h3>
<form id="q3_form" action="home/q3" method="POST">
    Company (Choose one): <br>
    <select id="q3_company" name="company" class="companies" required>
        {{#each company}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q3_result"></span>
</form>

<h3>Companies associated with a single role</h3>
<form id="q4_form" action="home/q4" method="POST">
    Role (Choose one): <br>
    <select id="q4_role" name="role" class="roles" required>
        {{#each role}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q4_result"></span>
</form>

<h3>Average manpower over the year for a single project role</h3>
<form id="q5_form" action="home/q5" method="POST">
    Project Role ID (Choose one): <br>
    <select id="q5_prid" name="project_role" class="prids" required>
        {{#each project_role}}
        <option value="{{id}}">{{id}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q5_result"></span>
</form>

<h3>Projects that a team is working on</h3>
<form id="q6_form" action="home/q6" method="POST">
    Team (Choose one): <br>
    <select id="q6_team" name="team" class="teams" required>
        {{#each team}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q6_result"></span>
</form>

<h3>Teams that are working on a project</h3>
<form id="q7_form" action="home/q7" method="POST">
    Project (Choose one): <br>
    <select id="q7_project" name="project" class="projects" required>
        {{#each project}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    <input type="submit"><br>
    <span id="q7_result"></span>
</form>